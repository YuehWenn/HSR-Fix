TODO
  4.修复只从trianglelist中读取的方法
  9.解决ps-t0替换后，去掉一个ps层后里面的东西坏掉的问题。
  11.添加GIMI中的nearest的TANGENT重新计算算法 KD-Tree
  13.Merge中有两段排序相同，需要提取成方法
  14.方法中自动根据ib hash读取对应文件中的element列表和位置，而不是写死在配置文件里，这个需要放到后面测试
  16.我们要把所有的文件移动方法单独分割开来，比如移动图片文件，接收一个要移动的图片文件的属性列表，这样方便在不同的游戏之间切换
  17.进入对话时，mod会出现bug，仍然需要过剧情来测试

特性：
    1.在dump的时候需要关闭mod，否则dump出来的文件名缺少打了mod部分的ib hash